# 独立監査レポート（再監査）

- 対象コミット：この文書を含む現行 `HEAD`
- 監査日：2026-08-19
- 監査方式：実装担当とは別の3エージェントによる読み取り監査。監査員は編集・コミットをしていない。
- 現在の判定：P0なし。自動実装ゲートと模試レッドチームはPASS。Franklin（教材）、Dewey（導線・保存）、Banach（模試・レッドチーム）の最終再監査もP0なし。ただし、実ブラウザE2E、全問題の意味的全件人手確認、G5実ユーザー結果が未検証なので、総合監査は条件付きであり、到達保証の最終PASSとはしない。

## ゲート判定

| ゲート | 判定 | 根拠 |
| --- | --- | --- |
| G1 自完結性 | PASS | 320概念、320ガイド、320レッスン、2,392問。数学IIIを含む。 |
| G2 内容品質 | 構造PASS／意味的レビュー未完 | X4-B717問と生成問題1,749問に数値・式・条件記号を要求し、全問の4択一意性・正答解説・テンプレート残存を検査。transfer/delayedの再表示重複は修正したが、生成問題の数学的意味・独立性は全件人手未検証。 |
| G3 習得証拠 | PASS | quick→standard→transfer→delayed、段階選択960、遅延320、誤答原因・時刻・問題IDを保存。 |
| G4 模試 | PASS | IA 3、IIBC 3、Math III 3の計9フォーム。100点、時間制限、誘導、未回答、途中復帰を実装。UIから3科目すべてを選択可能。 |
| G5 実ユーザー6割 | 未検証 | 実ユーザーが未見IA3回・IIBC3回を時間内に解いた結果が未提出。 |
| G6 数学III完結 | PASS | 46概念、138問、3フォーム。極限・微分・積分の統合フォームを含む。 |

## 3エージェント再監査

| 監査員 | P0 | ADHD自走 | UX障壁 | 共テ6割導線 |
| --- | --- | --- | --- | --- |
| Franklin（教材） | なし | 条件付き | 実機・全問意味レビュー未検証 | G5未検証 |
| Dewey（導線・保存） | なし | 条件付き | 2タブ・再起動・320px実機未検証 | G5未検証 |
| Banach（レッドチーム） | なし | 条件付き | 意味レビュー・実機未検証 | G5未検証 |

共通の条件は、実機ブラウザE2Eと、実ユーザーがIA-F1〜F3・IIBC-F1〜F3を初回提出するG5実測が未実施であること。したがって「6割を保証する」とは判定していない。

## 独立監査の結果

### A：数学コンテンツ

構造PASS。X4-B717問（239概念×3段階）はscope cardまたは手書き例題を元にし、生成問題1,749問の数値・式・条件記号なしは0件にした。全2392問、Math III46概念/138問、9フォームを確認した。概念ID、正答一意性、正答を含む解説、bulkのメタテンプレート残存、遅延再テストの汎用テンプレート残存を検査し、C-47（X4-B703〜705）も再確認した。全問題の数学的意味・転移の妥当性は全件人手未確認のため、G2は構造PASS／意味レビュー未完とする。

### B：学習導線・保存・インポート

実装・静的検査PASS。ガイド→例題→確認、誤答原因5種→retry、7日後再テスト、リロード/import、Math III導線、320px/キーボード静的条件、HTTP/static配信を検査した。storageイベントは複数キーを50msで合流し、同期直後の書き戻しを抑止する。共テ幹線はpriority=coreのI/A/II/B/Cに限定し、模試UIにはIA・IIBC・数学IIIの科目切替を追加した。橋渡しスキップは明示マーカーを保存して再開時の前提ロックと整合させた。前提F-01未達のF-02偽importは拒否し、正規のF-01証拠がある場合は受理した。模試結果には提出前解説非閲覧フラグを保存し、欠落した旧記録や`true`をG5合格として扱わない。実ブラウザの2タブ・再起動・320px操作は未検証である。

### C：試験エンジン境界・改ざん耐性

構造PASS（独立証明ではない）。未知・重複・非文字・4件選択、未来/逆転/制限時間改ざん、`Infinity`/`NaN`の現在時刻、未回答ID、`bySection`、設問結果台帳、余分キー、旧形式移行、全9フォーム正常履歴を検査した。現行形式では提出答案台帳から得点・設問結果・セクション結果を再計算するため、派生値だけの改ざんを拒否する。ただしGitHub Pagesのクライアント保存は第三者証明ではなく、決意ある利用者による完全なローカル偽造を防げないため、G5は外部観察者の記録を必須とする。

### D：保存競合・配信

実装・静的検査PASS。`navigator.locks`、localStorage fallback、非同期完了、3.5秒超IndexedDB遅延中のheartbeat、2タブ保存、リセット墓石、未来メタデータ、再読込、PWA/static/CSSを検査した。実ブラウザでの2タブ操作と再起動は未検証である。

## 機械ゲート

以下を対象ツリーで実行し、すべて終了コード0だった。

```text
npm test                         # 27/27 tests、curriculum/course/content/practice/exams/route/ui/buildを含む
npm run lint                     # PASS
npx tsc --noEmit                 # PASS
npm run build:github             # PASS
bash scripts/validate-artifact.sh # PASS
git diff --check                 # PASS
秘密情報パターン検索             # 該当なし
```

集計値は、概念320、共テ幹線239、ガイド320、レッスン320（手作業100/生成220）、問題2392、段階選択960、遅延320、具体的条件付きbulk717、Math III問題138、模試9フォーム、模試ソース問題156である。

## 未検証事項

- 監査環境にはChromium/Playwrightの実体がなく、実ブラウザのクリック、ファイル選択、再起動、2タブE2E、実機320px表示は未検証。
- G5は実ユーザーの学習結果で判定するため、監査員や自動生成データで代替していない。
- 全2,392問の数学的意味を人手で逐一再計算したわけではない。G2は構造全件検査、独立サンプル、生成元guide対応で判定している。生成問題の意味的独立性は引き続きレビュー対象である。

したがって、このリポジトリについては「自動実装ゲートPASS・独立監査P0なし。ただし総合監査は未完」と表現する。「誰でも6割を保証する」「ユーザーが6割達成済み」とは表現しない。実ブラウザ検証・意味的全件レビューとG5実測を完了するまで、到達保証のSUCCESSにはしない。
